# Hedge Fund v2 — API keys. Copy this file to ".env" and paste your keys after the "=".
# The .env file stays local and is never sent to chat. No quotes, no spaces around "=".

FRED_API_KEY=
TRADIER_ACCESS_TOKEN=
TRADIER_ENV=sandbox
ALPHAVANTAGE_API_KEY=
FMP_API_KEY=
